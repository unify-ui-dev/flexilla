---
title: Collapse
description: djkfjdf
---

## Installation

To get started, install Collapsible package, else you can skip this step if you've already installed `@flexilla/flexilla`.

<Tabs values={["npm", "yarn"]} autoHeight>
  <TabPanel value="npm" active>
  {/* <CodeInTab>
```bash 
npm i @flexilla/collapsible
```
  </CodeInTab> */}
  </TabPanel>
  <TabPanel value="yarn">
    {/* <CodeInTab>
```bash 
yarn add @flexilla/collapsible 
```
  </CodeInTab> */}
  </TabPanel>
</Tabs>

## Strucure

- `collapsibleElement` a `<div>` element (you can use any other tag that is a valid `HTMLElement`)
- `` a trigger

```html

```

Then import the component and init it

```js

```