---
import PreviewIframe from '@components/preact/PreviewIframe';

interface Props {
  url: string;
  name:string
}
const { url, name } = Astro.props;
---

<PreviewIframe prevUrl={url} name={name} client:only="preact"/>